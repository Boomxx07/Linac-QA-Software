<Window x:Class="Linac_QA_Software.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Linac_QA_Software"
        xmlns:views="clr-namespace:Linac_QA_Software.Views"
        xmlns:helpers="clr-namespace:Linac_QA_Software.Helpers.LinacQA"
        mc:Ignorable="d"
        Title="SCGH Linac QA" Height="900" Width="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15" Orientation="Vertical">
            <!-- Header -->
            <TextBlock Text="Annual Photons Linac QA" FontSize="26" FontWeight="Bold" Margin="0,0,0,15"/>

            <!-- Top Inputs -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Linac -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Linac"/>
                    <ComboBox ItemsSource="{Binding LinacOptions}"
                              SelectedItem="{Binding Linac}" />
                </StackPanel>

                <!-- Date -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Date"/>
                    <DatePicker SelectedDate="{Binding Date}"/>
                </StackPanel>

                <!-- Physicists -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Physicists"/>
                    <ListBox SelectionMode="Multiple"
                             ItemsSource="{Binding PhysicistList}"
                             helpers:ListBoxHelper.SelectedItems="{Binding SelectedPhysicists}"
                             Height="60" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Linearity START" FontSize="20" FontWeight="Bold"/>
            <views:LinearityView DataContext="{Binding LinearityVM}" />
            <TextBlock Text="Linearity END" FontSize="20" FontWeight="Bold"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Width="100" Margin="5" Command="{Binding SaveCommand}"/>
                <Button Content="Submit" Width="100" Margin="5" Command="{Binding SubmitCommand}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
