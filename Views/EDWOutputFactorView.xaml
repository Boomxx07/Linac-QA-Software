<UserControl x:Class="Linac_QA_Software.Views.EDWOutputFactorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Linac_QA_Software.ViewModels"
             xmlns:helpers="clr-namespace:Linac_QA_Software.Helpers"
             helpers:ScrollViewerExtension.BubbleMouseWheel="True"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="White"
             d:DataContext="{d:DesignInstance Type=viewmodels:EDWOutputFactorViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#E0FFFF"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="helpers:NumericInputBehavior.IsNumericOnly" Value="True"/>
            <Style.Triggers>
                <!-- Show red border when validation errors are present -->
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CalculatedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#E6E6FA"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding EnergyConfigs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <GroupBox Header="{Binding EnergyName}" Margin="10,0,10,20" Padding="10">
                        <StackPanel>
                            <TextBlock Text="{Binding MeasurementInstruction}" Margin="0,0,0,10" TextWrapping="Wrap"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="90"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                    <RowDefinition Height="28"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1" Background="#F0F0F0"/>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Wedge" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="Direction" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="3" Text="Readings (nC)" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="5" Text="Average (nC)" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="6" Text="EDW OF" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="7" Text="% Diff from Baseline" Style="{StaticResource HeaderTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="8" Text="Result" Style="{StaticResource HeaderTextBlockStyle}"/>

                                <Border Grid.Row="1" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,1"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="10x10 Open" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="N/A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Rows[0].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Rows[0].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding Rows[0].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="5" Text="{Binding Rows[0].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="6" Text="N/A" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="White"/>
                                <TextBox Grid.Row="1" Grid.Column="7" Text="N/A" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="White"/>
                                <TextBox Grid.Row="1" Grid.Column="8" Text="N/A" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="White"/>

                                <Border Grid.Row="2" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,0"/>
                                <TextBlock Grid.Row="2" Grid.RowSpan="2" Grid.Column="0" Text="15°" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="IN" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding Rows[1].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Rows[1].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding Rows[1].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="2" Grid.Column="5" Text="{Binding Rows[1].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="2" Grid.Column="6" Text="{Binding Wedge15Result.OutputFactor, StringFormat={}{0:F4}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="2" Grid.Column="7" Text="{Binding Wedge15Result.PercentDiff, StringFormat={}{0:F2}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="2" Grid.Column="8" Text="{Binding Wedge15Result.StatusText, Mode=OneWay}" Height="35">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource CalculatedTextBoxStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Wedge15Result.StatusText}" Value="FAIL">
                                                    <Setter Property="Background" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge15Result.StatusText}" Value="CAUTIONARY">
                                                    <Setter Property="Background" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge15Result.StatusText}" Value="OK">
                                                    <Setter Property="Background" Value="LawnGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Border Grid.Row="3" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,1"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="OUT" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="3" Grid.Column="2" Text="{Binding Rows[2].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding Rows[2].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="3" Grid.Column="4" Text="{Binding Rows[2].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="3" Grid.Column="5" Text="{Binding Rows[2].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>

                                <Border Grid.Row="4" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,0"/>
                                <TextBlock Grid.Row="4" Grid.RowSpan="2" Grid.Column="0" Text="30°" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="IN" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding Rows[3].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding Rows[3].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding Rows[3].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="4" Grid.Column="5" Text="{Binding Rows[3].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="4" Grid.Column="6" Text="{Binding Wedge30Result.OutputFactor, StringFormat={}{0:F4}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="4" Grid.Column="7" Text="{Binding Wedge30Result.PercentDiff, StringFormat={}{0:F2}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="4" Grid.Column="8" Text="{Binding Wedge30Result.StatusText, Mode=OneWay}" Height="35">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource CalculatedTextBoxStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Wedge30Result.StatusText}" Value="FAIL">
                                                    <Setter Property="Background" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge30Result.StatusText}" Value="CAUTIONARY">
                                                    <Setter Property="Background" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge30Result.StatusText}" Value="OK">
                                                    <Setter Property="Background" Value="LawnGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Border Grid.Row="5" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,1"/>
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="OUT" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="5" Grid.Column="2" Text="{Binding Rows[4].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="5" Grid.Column="3" Text="{Binding Rows[4].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="5" Grid.Column="4" Text="{Binding Rows[4].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="5" Grid.Column="5" Text="{Binding Rows[4].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>

                                <Border Grid.Row="6" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,0"/>
                                <TextBlock Grid.Row="6" Grid.RowSpan="2" Grid.Column="0" Text="45°" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="IN" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding Rows[5].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="6" Grid.Column="3" Text="{Binding Rows[5].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="6" Grid.Column="4" Text="{Binding Rows[5].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="6" Grid.Column="5" Text="{Binding Rows[5].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="6" Grid.Column="6" Text="{Binding Wedge45Result.OutputFactor, StringFormat={}{0:F4}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="6" Grid.Column="7" Text="{Binding Wedge45Result.PercentDiff, StringFormat={}{0:F2}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="6" Grid.Column="8" Text="{Binding Wedge45Result.StatusText, Mode=OneWay}" Height="35">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource CalculatedTextBoxStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Wedge45Result.StatusText}" Value="FAIL">
                                                    <Setter Property="Background" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge45Result.StatusText}" Value="CAUTIONARY">
                                                    <Setter Property="Background" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge45Result.StatusText}" Value="OK">
                                                    <Setter Property="Background" Value="LawnGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Border Grid.Row="7" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,1"/>
                                <TextBlock Grid.Row="7" Grid.Column="1" Text="OUT" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="7" Grid.Column="2" Text="{Binding Rows[6].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="7" Grid.Column="3" Text="{Binding Rows[6].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="7" Grid.Column="4" Text="{Binding Rows[6].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="7" Grid.Column="5" Text="{Binding Rows[6].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>

                                <Border Grid.Row="8" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,0"/>
                                <TextBlock Grid.Row="8" Grid.RowSpan="2" Grid.Column="0" Text="60°" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Grid.Row="8" Grid.Column="1" Text="IN" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="8" Grid.Column="2" Text="{Binding Rows[7].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="8" Grid.Column="3" Text="{Binding Rows[7].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="8" Grid.Column="4" Text="{Binding Rows[7].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="8" Grid.Column="5" Text="{Binding Rows[7].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="8" Grid.Column="6" Text="{Binding Wedge60Result.OutputFactor, StringFormat={}{0:F4}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="8" Grid.Column="7" Text="{Binding Wedge60Result.PercentDiff, StringFormat={}{0:F2}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}" Height="35"/>
                                <TextBox Grid.RowSpan="2" Grid.Row="8" Grid.Column="8" Text="{Binding Wedge60Result.StatusText, Mode=OneWay}" Height="35">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource CalculatedTextBoxStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Wedge60Result.StatusText}" Value="FAIL">
                                                    <Setter Property="Background" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge60Result.StatusText}" Value="CAUTIONARY">
                                                    <Setter Property="Background" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Wedge60Result.StatusText}" Value="OK">
                                                    <Setter Property="Background" Value="LawnGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Border Grid.Row="9" Grid.ColumnSpan="9" BorderBrush="Black" BorderThickness="1,0,1,1"/>
                                <TextBlock Grid.Row="9" Grid.Column="1" Text="OUT" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="9" Grid.Column="2" Text="{Binding Rows[8].Reading1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="9" Grid.Column="3" Text="{Binding Rows[8].Reading2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="9" Grid.Column="4" Text="{Binding Rows[8].Reading3, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBox Grid.Row="9" Grid.Column="5" Text="{Binding Rows[8].Average, StringFormat={}{0:F3}, Mode=OneWay}" Style="{StaticResource CalculatedTextBoxStyle}"/>

                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>

</UserControl>
