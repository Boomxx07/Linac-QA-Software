<UserControl x:Class="Linac_QA_Software.Views.LinearityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Linac_QA_Software.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Leakage Section -->
        <GroupBox Header="Leakage" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Headers -->
                <TextBlock Text="" Grid.Row="0" Grid.Column="0"/>
                <TextBlock Text="Time (s)" Grid.Row="0" Grid.Column="1"/>
                <TextBlock Text="Reading (nC)" Grid.Row="0" Grid.Column="2"/>

                <!-- Start -->
                <TextBlock Text="Start" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding StartTime}" Grid.Row="1" Grid.Column="1"/>
                <TextBox Text="{Binding StartReading}" Grid.Row="1" Grid.Column="2"/>

                <!-- End -->
                <TextBlock Text="End" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding EndTime}" Grid.Row="2" Grid.Column="1"/>
                <TextBox Text="{Binding EndReading}" Grid.Row="2" Grid.Column="2"/>

                <!-- Result -->
                <StackPanel Grid.Row="1" Grid.Column="4" Grid.RowSpan="2">
                    <TextBlock Text="Leakage (nC/s)"/>
                    <TextBlock Text="{Binding LeakageRate}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Measurements Section -->
        <GroupBox Header="Measurements" Grid.Row="1">
            <DataGrid ItemsSource="{Binding Measurements}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MU" Binding="{Binding MU}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Reading 1" Binding="{Binding Readings[0], Mode=TwoWay}"/>
                    <DataGridTextColumn Header="Reading 2" Binding="{Binding Readings[1], Mode=TwoWay}"/>
                    <DataGridTextColumn Header="Reading 3" Binding="{Binding Readings[2], Mode=TwoWay}"/>
                    <DataGridTextColumn Header="Average" Binding="{Binding Average}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Leakage Corrected" Binding="{Binding LeakageCorrectedValue}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Reading/MU" Binding="{Binding ReadingPerMUValue}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="% Diff vs 200" Binding="{Binding PercentDiffValue}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Result" Binding="{Binding Result}" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

    </Grid>
</UserControl>
